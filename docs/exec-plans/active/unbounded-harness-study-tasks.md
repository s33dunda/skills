# Unbounded Harness Study Task Manifest

Generated: 2026-05-05

This manifest defines the N=1 fast-pass battery for `docs/exec-plans/active/unbounded-harness-study.md`.

## Scoring Rubric

Each run receives four 0-3 scores:

- `navigation_score`: found the right subsystem, ownership boundary, and relevant tests.
- `scope_score`: avoided broad unrelated edits or vague answers.
- `validation_score`: used direct checks, focused tests, or a credible no-code verification plan.
- `handoff_score`: explained decisions, risks, and evidence clearly.

Navigation tasks are no-code. Implementation and maintenance tasks may edit code, tests, docs, or harness files inside the run sandbox.

## Tasks

| Task ID | Tier | Slot | Repo | Type | Prompt | Answer-key anchors | Validation anchors |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `httpx-navigation-current` | medium | `httpx` | `psf/requests` | navigation | Explain how Requests prepares outbound request bodies when the body may be text, bytes, file-like, or streamed. Identify where you would change behavior for body length calculation and which tests would prove it. Do not edit files. | Strong answer names `PreparedRequest.prepare_body`, `super_len`, request utility helpers, interaction with headers, and likely tests under request/utils coverage. | No diff; answer should include direct repro shape and focused pytest targets such as request utility/body preparation tests. |
| `httpx-implementation-current` | medium | `httpx` | `psf/requests` | implementation | Add a small user-facing improvement so prepared requests handle text-like in-memory bodies more predictably when calculating `Content-Length`. Keep the change narrow and add focused regression coverage. | Strong solution stays near body-length preparation, avoids global encoding rewrites, and records pre/post direct header behavior. | Direct `PreparedRequest` repro plus focused request utility/model tests; `PYTHONPATH=src` when needed. |
| `httpx-maintenance-current` | medium | `httpx` | `psf/requests` | maintenance | Inspect the authentication, cookie, body-preparation, or charset-detection surfaces and add one high-leverage regression guardrail that would prevent a subtle future bug. Justify why that guardrail is the right one. | Strong solution chooses one existing fragile surface, adds focused tests without broad cleanup, and documents why it is a guardrail rather than a feature. | Focused pytest target for the chosen surface plus `git diff --check`. |
| `typer-navigation-current` | medium | `typer` | `fastapi/typer` | navigation | Explain how Typer connects Click command construction, autocompletion, and callback/result-callback behavior. Identify where a fix should land for context propagation bugs and which tests should cover it. Do not edit files. | Strong answer names command construction in Typer, Click callback/autocompletion integration, completion tests, and CLI runner tests without overclaiming shell-specific behavior. | No diff; answer should distinguish Python-level completion behavior from shell wrapper behavior. |
| `typer-implementation-current` | medium | `typer` | `fastapi/typer` | implementation | Make a small behavior improvement to callback/autocompletion context handling that a CLI user would notice. Keep it compatible with the pinned Click/Typer baseline and add focused tests. | Strong solution stays in Typer command/callback construction and does not rewrite shell completion broadly. | Direct `CliRunner` or completion repro plus focused tests with `PYTHONPATH=.` if editable install is not practical. |
| `typer-maintenance-current` | medium | `typer` | `fastapi/typer` | maintenance | Inspect the tutorial/test coverage around options, callbacks, and completion. Add one guardrail test or docs-test alignment that would make future CLI behavior regressions easier to catch. | Strong solution selects one local behavior seam, adds a targeted test or doc/test alignment, and avoids broad tutorial rewrites. | Focused Typer test node plus direct command output check when possible. |
| `structlog-navigation-current` | medium | `structlog` | `hynek/structlog` | navigation | Explain how structlog preserves callsite information across sync and async logging paths. Identify the owner modules and tests for changing thread/function/line attribution. Do not edit files. | Strong answer names callsite processors, async wrapper/stdlib paths, and relevant processor tests; it distinguishes desired caller origin from worker-thread execution details. | No diff; answer should include focused `CallsiteParameterAdder` async test targets and risks around stdlib/native paths. |
| `structlog-implementation-current` | medium | `structlog` | `hynek/structlog` | implementation | Add a small improvement to exception or callsite processing that makes async logging output more consistent with sync logging. Keep the implementation localized and add focused tests. | Strong solution stays near processors/stdlib async boundaries, avoids broad logging pipeline rewrites, and tests async plus sync behavior when relevant. | Direct repro plus focused tests with `uv run --extra tests` when available. |
| `structlog-maintenance-current` | medium | `structlog` | `hynek/structlog` | maintenance | Inspect processor tests for duplicated edge-case setup. Add one maintainability guardrail that makes a future processor regression easier to diagnose without changing public behavior. | Strong solution adds focused test helpers or parametrized coverage near existing tests and leaves public behavior unchanged. | Focused processor tests plus `git diff --check`. |
| `fastapi-navigation-current` | heavy | `fastapi` | `fastapi/fastapi` | navigation | Explain how FastAPI turns endpoint signatures, dependencies, forms, and routing metadata into validation and OpenAPI output. Identify likely files/tests for changing form model or operation-id behavior. Do not edit files. | Strong answer names routing/dependency utility surfaces, OpenAPI generation, form model tests, and operation ID tests; it separates request runtime behavior from schema generation. | No diff; answer should include focused test files for forms, annotations, and operation IDs. |
| `fastapi-implementation-current` | heavy | `fastapi` | `fastapi/fastapi` | implementation | Add a small consistency improvement around how FastAPI derives or preserves route metadata for generated OpenAPI output. Keep behavior backwards-compatible and add focused tests. | Strong solution stays near route metadata/OpenAPI generation, avoids unrelated dependency rewrites, and handles duplicate or multi-method cases carefully. | Direct OpenAPI repro plus focused tests such as operation-id or generated-schema tests. |
| `fastapi-maintenance-current` | heavy | `fastapi` | `fastapi/fastapi` | maintenance | Inspect FastAPI tests around forms, annotations, dependencies, and route metadata. Add one high-leverage regression guardrail that would catch a likely future schema/runtime mismatch. | Strong solution adds a targeted test near an existing feature seam and explains the mismatch it guards against. | Focused pytest target plus direct schema/runtime assertion. |
| `celery-navigation-current` | heavy | `celery` | `celery/celery` | navigation | Explain how Celery worker startup/shutdown, broker connection retry, errbacks, and native delayed delivery are organized. Identify where you would change each and how to validate without real brokers. Do not edit files. | Strong answer names worker startup/shutdown owner tests, backend errback flow, native delayed delivery unit tests, and broker-free validation strategy. | No diff; answer should include unit-level validation targets and external-service risks. |
| `celery-implementation-current` | heavy | `celery` | `celery/celery` | implementation | Add one small operational improvement to worker startup/shutdown or delayed-delivery configuration that can be validated with unit tests and no live broker. | Strong solution chooses one operational seam, adds setting or behavior coverage, and avoids requiring SQS/RabbitMQ side effects. | Focused Celery unit tests; strict marker/env caveats documented when encountered. |
| `celery-maintenance-current` | heavy | `celery` | `celery/celery` | maintenance | Inspect the worker/backend/native-delayed-delivery tests and add one guardrail that would make an operational regression easier to catch locally. | Strong solution adds unit-level coverage or helper cleanup near existing tests and avoids broad refactors. | Focused unit tests plus `git diff --check`. |
| `rich-navigation-current` | heavy | `rich` | `Textualize/rich` | navigation | Explain how Rich renderables receive and propagate `ConsoleOptions`, and how text/traceback/logging rendering paths preserve formatting. Identify likely files/tests for width, ANSI, traceback, and file-link bugs. Do not edit files. | Strong answer names `ConsoleOptions`, render protocol surfaces, text ANSI parsing, traceback/logging owner files, and relevant tests. | No diff; answer should include focused tests for console/options, text, traceback, logging, or table rendering. |
| `rich-implementation-current` | heavy | `rich` | `Textualize/rich` | implementation | Add a small rendering consistency improvement where an option or formatting override should propagate through nested renderables. Keep it local and add focused tests. | Strong solution stays within a renderable/options propagation seam and avoids sweeping render protocol changes. | Direct render repro plus focused Rich tests. |
| `rich-maintenance-current` | heavy | `rich` | `Textualize/rich` | maintenance | Inspect tests around text parsing, tracebacks, logging, or console options. Add one guardrail test that would prevent a subtle rendering regression. | Strong solution adds focused coverage around an existing rendering seam and explains why the chosen case is high leverage. | Focused pytest target plus direct render assertion. |
